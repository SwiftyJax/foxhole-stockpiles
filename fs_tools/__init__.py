"""fs_tools - Foxhole Stockpiles database and template tools.

Standalone package (sibling to ``fs_ocr``) providing the ``fs-tools`` CLI and
GUI for building and inspecting template/catalog databases. Depends on shared
code in ``foxhole_stockpiles``; the main package must not import from here.
"""
